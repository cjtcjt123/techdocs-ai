node_modules/
.expo/
dist/
# 下面这些由 `expo prebuild` 在 CI 生成，不入库
ios/
android/
# CI 运行时由 scripts/make-export-options.py 生成
exportOptions.plist
# 本地构建产物（IPA），不入库
release/
npm-debug.log*
yarn-error.log*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.DS_Store
