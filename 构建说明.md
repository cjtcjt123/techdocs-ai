# 技术资料 AI 助手 · 构建与运行说明

项目基于 **Expo SDK 52 + React Native**，MVP-0 已实现：手机本地资料库（SQLite）、文档导入、云端/NAS 三源模型问答、来源引用、需求符合性检查。

## 一、本地预览（最快看到效果，免签名）
```bash
npm install
npm start          # 启动 Expo Dev Server
```
- iPhone 装 **Expo Go** → 扫码即可运行（无需开发者账号、无需签名）。
- 或用浏览器访问 `http://<本机IP>:8081`（零账号预览，热重载正常）。

## 二、出「可安装 IPA」（你自签）
你已确认：签名你来做，我只出 **未签名 IPA**。两条路：

### 方案 A：GitHub Actions 免费 macOS Runner（推荐，无需任何苹果凭据）
1. 把本目录推到 GitHub 仓库（需先 `git init` 已就绪）。
2. 仓库 **Settings → Actions → General → Workflow permissions** 设为 `Read and write`。
3. 进入 **Actions → build-unsigned-ios → Run workflow**。
4. 跑完在 Artifacts 下载 `unsigned-ipa`，里面是 `techdocs-ai.ipa`（未签名）。
5. 你用 AltStore / SideStore / Apple Configurator 以你的证书自签安装（免费 Apple ID 即可）。

> 工作流文件：`.github/workflows/build-unsigned-ios.yml`，使用 `maxgfr/expo-unsigned-ios` 动作，云端用 Xcode 编译并关闭签名，自动收集 IPA 产物。

### 方案 B：EAS 云端构建（需 $99 付费开发者账号）
EAS 提交 iOS 构建**必须配置苹果签名凭据**（哪怕自定义脚本关掉签名，提交环节仍要校验）。
1. 去 https://expo.dev/accounts/cjtallen/projects/techdocs-ai/credentials 添加你的 Apple 账号。
2. `eas build -p ios -e unsigned`（已配 `.eas/build/unsigned-ios.yml` 关闭签名）。
3. 或配 `preview`/`production` 档走 EAS 直接签好。

## 三、配置 API（首次打开 App）
「我的 → API 与模型」：
- **官方 API**：选供应商（OpenAI/Claude/通义/智谱/自定义），填模型名与 API Key（仅存本机 secure-store）。
- **NAS 后端**：选「NAS后端」，填你 Ollama 的 `http://<NAS>:11434/v1` 地址与模型名（需你后端就绪）。
- **数据策略**：仅本地 / 本地+同步NAS / 走NAS后端（同步功能后续接入）。

## 四、已知 MVP 限制（后续迭代）
- PDF / Word 暂未接入文本解析引擎：导入后标记「待解析」，仅入库元数据；TXT/MD 可全文检索。
- 手机本地离线模型：UI 可切换但后端未接入（后续）。
- NAS 同步、OCR、Excel/PPT 为后续迭代。
- 对话记录当前存内存，重启清空（后续落库）。
- 图片作为附件可携带，但暂不支持图片内文字识别。
