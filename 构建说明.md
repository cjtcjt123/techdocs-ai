# 技术资料 AI 助手 · 构建与运行说明

**Expo SDK 54**（React Native 0.81.5 / React 19.1.0 / TypeScript），iOS 自用、不上架。

当前能力：手机本地资料库（SQLite）·**文档解析入库**（PDF / Word / TXT / Markdown）·
**混合检索**（BM25 关键词 + 向量语义 + RRF 融合）· 多会话问答与来源引用 · 问答内临时附件 ·
需求符合性检查与导出 · **个人经验库**（记录后回灌进问答）·
双源模型（官方 API / 手机本地 GGUF）· 可选 NAS（解析 / 向量 / 同步）。

---

## 一、本地预览（最快看到效果，免签名）

```bash
npm install
npm start          # 启动 Expo Dev Server
```

- **浏览器**：`http://localhost:8081`（零账号、热重载正常，最适合迭代 UI）。
- **真机**：iPhone 装 **Expo Go** → 扫码即可（无需开发者账号、无需签名）。
  注意 Expo Go 自 SDK 57 起要求 App 与 CLI 登录同一个 Expo 账号，本机 SDK 54 不受影响。

### ⚠️ 本机两个坑（换机器可忽略）

1. **本机拦截 rename 覆盖已存在文件（EPERM）**，会让 Expo CLI 写 `~/.expo/state.json` 失败，
   表现为 dev server 对手机返回 HTTP 500。必须挂降级补丁启动：

   ```bash
   NODE_OPTIONS="--require $(pwd)/scripts/patch-expo-fs.js" npm start
   ```

   缓存出问题时清一次：`rm -rf ~/.expo/native-modules-cache`。

2. **web 依赖必须在启动 dev server 之前装好**，否则浏览器拿到的是 manifest JSON：
   ```bash
   npx expo install react-dom react-native-web @expo/metro-runtime
   ```

---

## 二、出「可安装 IPA」（未签名，自己签）

签名自己做，这里只出**未签名 IPA**。两条路：

### 方案 A：GitHub Actions 免费 Runner（推荐，无需任何苹果凭据）

工作流 `.github/workflows/build-ios.yml` 是**双路兜底**：仓库里有签名 Secrets 就出签名 IPA，
没有就出未签名 IPA，产物统一发到 GitHub Release。

1. 仓库 **Settings → Actions → General → Workflow permissions** 设为 `Read and write`。
2. **Actions → Build iOS IPA → Run workflow**。
3. 跑完去该次运行的 **Release** 里下载 IPA（未签名版形如 `techdocs-ai-unsigned.ipa`）。
4. 用 AltStore / SideStore / 自签工具安装；本机已验证 **TrollStore** 可直接装未签名包。

### 方案 B：EAS 云端构建（需 $99 付费开发者账号）

EAS 提交 iOS 构建**必须配置苹果签名凭据**（哪怕自定义脚本关掉签名，提交环节仍会校验）。
配置在 `.eas/build/unsigned-ios.yml`，命令 `eas build -p ios -e unsigned`。

---

## 三、首次配置

**「我的」页**（工作台右上角 ⚙ 进入）：

| 想做什么 | 去哪 |
|---|---|
| 填 API Key / 换模型 | 模型来源 + API 配置（Key 只存本机 secure-store） |
| 用手机本地离线模型 | 📦 手机本地模型（下载 / 导入 / 加载） |
| 让 PDF 解析更准 | 文档解析服务（填 NAS 上的 nas-parse 地址） |
| 开启语义检索（换个说法也能搜到） | 语义检索（嵌入服务） |
| 连 NAS 拉资料 | NAS 连接 + 数据策略 |
| 隐私锁 / 离线模式 / 云端确认 | 隐私与安全（见下方说明） |

**数据策略**三档：仅本地 / 本地+同步 NAS / 走 NAS 后端（手机仅客户端）。

**两个隐私开关的实际行为**（都已真实接线，不是摆设）：

- **离线模式**：拦下所有联网动作并说明原因（云端模型、NAS 解析、NAS 同步、语义检索、模型下载）。
  此时仍可用的组合是「手机本地模型 + 手机本地解析 + 关键词检索」，全程不联网。
- **云端调用需确认**：在资料离开你的网络前弹窗确认。判定看目标地址 ——
  NAS、`192.168.x` 这类内网地址不询问；只有对外地址才拦。

---

## 四、本地服务（可选，不配也能完整使用）

| 服务 | 目录 | 端口 | 用途 |
|---|---|---|---|
| nas-parse | `nas-parse/` | 8787 | PyMuPDF 高精度解析 + `/embed` 向量（bge-small-zh-v1.5，512 维） |
| docvault | `docvault/` | 8790 | 用户 / 令牌 / API Key 管理页（**App 端尚未接入**） |

两个都是 FastAPI，自带 `selfcheck.py` 自检；`docker compose up -d --build` 即可起。

---

## 五、已知限制（未来迭代）

- **扫描件 OCR 未做**：整页图片的 PDF 提不出文字，会标成「无文本」（`partial`）。
- **Excel / PPT / 网页导入未做**；`.doc`（旧版二进制）不支持，请另存为 `.docx`。
- **多设备同步未做**；经验案例也暂无一键导出 —— 案例是手工积累、丢了不可再生。
- **账号与权限未接**：后端 docvault 已就绪，App 端还是单机模式（当前定的是单人自用）。
- 图片附件可携带，但**不支持图片内文字识别**（同 OCR）。
- **经验库只做到「记录 + 回灌」**：还没有案例列表页，所以记进去之后看不到、改不了、删不了
  （方案里的 P1，下一步补）。

---

## 六、测试与自检

```bash
# 纯逻辑 + 真实检索（Node 直接跑，不需要 dev server）
node scripts/test-retrieval.mjs      # 分词 / BM25
node scripts/test-rag.mjs            # 多路召回 + RRF 融合
node scripts/test-cases.mjs          # 经验库（含「草稿不进检索」的门禁）
node scripts/test-embedding.mjs      # 嵌入层契约（两套服务形状）
node scripts/test-local-models.mjs   # GGUF 头解析 / 机型适配

# 浏览器端到端（需先起 dev server + 带调试端口的 Chrome，模型用离线桩）
node scripts/web-import-check.js     # 导入 → 检索 → 问答
node scripts/web-case-check.js       # 经验库闭环（记录 → 回灌命中）
node scripts/web-batch-check.js      # 资料库批量操作
node scripts/check-text-clip.js      # 文字裁切自检（中文下半截被裁）
```

类型检查：`./node_modules/.bin/tsc -p tsconfig.check.json`（应为 0 错）。
